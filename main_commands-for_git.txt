Github Account Link with Git:

1) git config --global user.name "USERNAME" = for set Github Username
2) git config --global user.email "GITHUB EMAIL" = for set Github Email address

3) git config --list = show what we change or set in git 


Commands of Git Bash :
1) git clone = for clone/copy Repository to local machine
2) git status = for chack changes we done or show status of our Repository 

Git Status 4 Type: 
1) Untract = New File that git dosen't check yet
2) Modified = changed or changes that we made
3) Unmodified = Unchanged or Not changes anything in file 
4) Staged = file is ready to be commited (that phase after Add changes before Commite)

Add & Commite:
1) add: adds new or changed file in your working directory to the Git staging area.(middle stage of Unchanged and Commite Stage)
->  git add "FILE_NAME" (single File)
->  git add . (all file)

2) Commite: it is the record of change.
->git commit -m "some message"

Note: Still Not showing Data on Github Website 
for that 

Push Command :
uplod local Repo to remote Repo

->git push origin main = origin is Alias of Remote Repo (Github Pero name) main is Branch name


Init Command:
1) git init = create a new Git Repo
Before Push check this.....

->git remote add origin <link> = Add new remote
->git remote -v = to verify remote
->git branch = check Branch 
->git branch -M <branch name> = change Branch name
->git push origin main
->git push -u origin main = (Set Up stream)set Upcomming all Push in Origin main 

Branches:

->git branch = for chech current branch
->git branch -M <branch name> = change Branch name
->git chackout <branch name> = to navigate diff.. branch (Chnage Branch) 
->git checkout -b <New Branch name> = to create a new Branch
->git branch -d <branch name> = Delete Branch 

Merge Branch:
Way-1 
->git diff <branch name> = Chech Diffrances to another Branch
->git merge <branch name> = to merge 2 Branche

Way-2 (with github )
Create PR (Pull Request)

To show on local machine we Pull the main Branch on Local machine 
->git pull origin main = featch and Download content from remote repo and immediately Update the Local Repo to match that cointent

Resolving Merge Conflict:
to chech any confliction are not 
->git diff <branch name> = to chech changes/campare changes

Hello karan

Undoing Changes:

1)we add the changes but not commit it.
->git reset <file name> = to reset perticuler file 

2)we commit unwanted changes
->git reset HEAD~1 = back one Commit state (second last Commit state)

3)we can go Multiple step Commit
->git reset <Commit Hash> = we can go bach perticular Commit state (Commit hash -> git log)
changes are not to save
